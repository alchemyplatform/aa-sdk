---
title: prepareSign
description: Overview of the prepareSign function
slug: wallets/reference/wallet-apis/functions/prepareSign
layout: reference
---

{/* This file is auto-generated by TypeDoc. Do not edit manually. */}

```ts
function prepareSign<TAccount>(client, params): Promise<{
  chainId: `0x${string}`;
  signatureRequest:   | {
     data:   | string
        | {
        raw: `0x${string}`;
      };
     type: "personal_sign";
   }
     | {
     data: {
        domain?: {
           chainId?: number;
           name?: string;
           salt?: `0x${string}`;
           verifyingContract?: `0x${string}`;
           version?: string;
        };
        message: Record<string, unknown>;
        primaryType: string;
        types: Record<string, object[]>;
     };
     type: "eth_signTypedData_v4";
   };
}>;
```

Defined in: [packages/wallet-apis/src/actions/prepareSign.ts:41](https://github.com/alchemyplatform/aa-sdk/blob/665ba335688b916e76a329c8db70db2297d08029/packages/wallet-apis/src/actions/prepareSign.ts#L41)

Prepares a signature request for signing messages or transactions.

## Example

```ts
// Prepare a message to be signed
const result = await client.prepareSign({
   from: "0x1234...",
   type: "personal_sign",
   data: "Hello, world!",
});
```

## Type Parameters

<table>
  <thead>
    <tr>
      <th align="left">Type Parameter</th>
      <th align="left">Default type</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `TAccount` *extends* `` `0x${string}` `` | `undefined`
      </td>

      <td>
        `` `0x${string}` `` | `undefined`
      </td>
    </tr>
  </tbody>
</table>

## Parameters

<table>
  <thead>
    <tr>
      <th align="left">Parameter</th>
      <th align="left">Type</th>
      <th align="left">Description</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `client`
      </td>

      <td>
        `InnerWalletApiClient`
      </td>

      <td>
        The wallet API client to use for the request
      </td>
    </tr>

    <tr>
      <td>
        `params`
      </td>

      <td>
        \{ \[K in string | number | symbol]: (Omit\<Omit\<\{ capabilities?: \{ permissions: \{ context: \`0x$\{(...)}\` } | \{ sessionId: \`0x$\{(...)}\`; signature: \`0x$\{(...)}\` } }; chainId: \`0x$\{string}\`; from: \`0x$\{string}\`; signatureRequest: \{ data: string | \{ raw: \`0x$\{(...)}\` }; type: "personal\_sign" } | \{ data: \{ domain?: \{ chainId?: ...; name?: ...; salt?: ...; verifyingContract?: ...; version?: ... }; message: Record\<string, unknown>; primaryType: string; types: Record\<string, (...)\[]> }; type: "eth\_signTypedData\_v4" } }, "from">, "chainId"> & \{ chainId?: \`0x$\{string}\` } & (IsUndefined\<TAccount> extends true ? \{ from: \`0x$\{string}\` } : \{ from?: undefined }))\[K] }
      </td>

      <td>
        Parameters for preparing the signature request
      </td>
    </tr>
  </tbody>
</table>

## Returns

`Promise`\<\{
`chainId`: `` `0x${string}` ``;
`signatureRequest`:   | \{
`data`:   | `string`
| \{
`raw`: `` `0x${string}` ``;
};
`type`: `"personal_sign"`;
}
| \{
`data`: \{
`domain?`: \{
`chainId?`: `number`;
`name?`: `string`;
`salt?`: `` `0x${string}` ``;
`verifyingContract?`: `` `0x${string}` ``;
`version?`: `string`;
};
`message`: `Record`\<`string`, `unknown`>;
`primaryType`: `string`;
`types`: `Record`\<`string`, `object`\[]>;
};
`type`: `"eth_signTypedData_v4"`;
};
}>

A Promise that resolves to the prepare sign result containing a signature request
