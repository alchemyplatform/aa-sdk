---
title: listAccounts
description: Overview of the listAccounts function
slug: wallets/reference/wallet-apis/functions/listAccounts
layout: reference
---

{/* This file is auto-generated by TypeDoc. Do not edit manually. */}

```ts
function listAccounts(client, params): Promise<{
  accounts: object[];
  meta: {
     after: string | null;
     totalCount: number;
  };
}>;
```

Defined in: [packages/wallet-apis/src/actions/listAccounts.ts:48](https://github.com/alchemyplatform/aa-sdk/blob/665ba335688b916e76a329c8db70db2297d08029/packages/wallet-apis/src/actions/listAccounts.ts#L48)

Lists all smart accounts for a given signer using the wallet API client.

## Example

```ts
// Fetch the first page of accounts
const firstPage = await client.listAccounts({
  signerAddress: "0x123...",
  limit: 10
});

// If an 'after' cursor exists, use it to fetch the next page
const nextPage = await client.listAccounts({
  signerAddress: "0x123...",
  limit: 10,
  after: firstPage.meta.after
});
```

## Parameters

<table>
  <thead>
    <tr>
      <th align="left">Parameter</th>
      <th align="left">Type</th>
      <th align="left">Description</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `client`
      </td>

      <td>
        `InnerWalletApiClient`
      </td>

      <td>
        The wallet API client to use for the request
      </td>
    </tr>

    <tr>
      <td>
        `params`
      </td>

      <td>
        \{ `after?`: `string`; `limit?`: `number`; `signerAddress?`: `` `0x${string}` ``; }
      </td>

      <td>
        Parameters for listing accounts
      </td>
    </tr>

    <tr>
      <td>
        `params.after?`
      </td>

      <td>
        `string`
      </td>

      <td>
        Optional pagination cursor for fetching subsequent pages
      </td>
    </tr>

    <tr>
      <td>
        `params.limit?`
      </td>

      <td>
        `number`
      </td>

      <td>
        Optional maximum number of accounts to return (default: 100, max: 100)
      </td>
    </tr>

    <tr>
      <td>
        `params.signerAddress?`
      </td>

      <td>
        `` `0x${string}` ``
      </td>

      <td>
        The address of the signer to list accounts for
      </td>
    </tr>
  </tbody>
</table>

## Returns

`Promise`\<\{
`accounts`: `object`\[];
`meta`: \{
`after`: `string` | `null`;
`totalCount`: `number`;
};
}>

A Promise that resolves to the list of accounts and pagination metadata
