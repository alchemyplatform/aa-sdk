---
title: PLACEHOLDER_DELEGATION_ADDRESS
description: "Stands in for the address of a contract that isn't deployed yet, so an unreleased version can be registered before its deployment lands.  `zeroAddress` rather than a plausible-looking address: it can't be mistaken for a real deployment, and `isAddressEqual(addr, zeroAddress)` is how the registry test finds these entries."
slug: wallets/reference/smart-accounts/variables/PLACEHOLDER_DELEGATION_ADDRESS
layout: reference
---

{/* This file is auto-generated by TypeDoc. Do not edit manually. */}

```ts
const PLACEHOLDER_DELEGATION_ADDRESS: "0x0000000000000000000000000000000000000000" =
  zeroAddress;
```

Defined in: [packages/smart-accounts/src/ma-v2/mav2StaticImpl.ts:32](https://github.com/alchemyplatform/aa-sdk/blob/main/packages/smart-accounts/src/ma-v2/mav2StaticImpl.ts#L32)

Stands in for the address of a contract that isn't deployed yet, so an
unreleased version can be registered before its deployment lands.

`zeroAddress` rather than a plausible-looking address: it can't be mistaken
for a real deployment, and `isAddressEqual(addr, zeroAddress)` is how the
registry test finds these entries.
