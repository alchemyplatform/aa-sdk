---
# This file is autogenerated
title: signTypedData
description: Overview of the signTypedData method
slug: wallets/reference/alchemy/wallet-apis/functions/signTypedData
---

Signs typed data (EIP-712) using the smart account.
This method requests the account associated with the signer and uses it to sign the typed data.

## Import

```ts
import { signTypedData } from "@alchemy/wallet-apis";
```

## Usage

```ts
// Sign typed data
const signature = await client.signTypedData({
  domain: {
    name: "Example DApp",
    version: "1",
    chainId: 1,
    verifyingContract: "0xCcCCccccCCCCcCCCCCCcCcCccCcCCCcCcccccccC",
  },
  types: {
    Person: [
      { name: "name", type: "string" },
      { name: "wallet", type: "address" },
    ],
  },
  primaryType: "Person",
  message: {
    name: "John Doe",
    wallet: "0xAaAaAaAaAaAaAaAaAaAAAAAAAAaaaAaAaAaaAaAa",
  },
});
```

## Parameters

### client

`InnerWalletApiClient`

- The wallet API client to use for the request

### params

`TypedDataDefinition`

- The typed data to sign, following EIP-712 format

## Returns

`Promise<SignTypedDataResult>`
A Promise that resolves to the signature as a hex string
