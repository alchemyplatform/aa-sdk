---
# This file is autogenerated

title: AlchemyWebSigner
description: Overview of the AlchemyWebSigner method
slug: wallets/reference/account-kit/signer/classes/AlchemyWebSigner/constructor
---

Initializes an instance with the provided Alchemy signer parameters after parsing them with a schema.

{" "}

<Note>
  `AlchemyWebSigner` extends `BaseAlchemySigner`, see the docs for
  BaseAlchemySigner for all supported methods.
</Note>

## Import

```ts
import { AlchemyWebSigner } from "@account-kit/signer";
```

## Usage

```ts
import { AlchemyWebSigner } from "@account-kit/signer";

const signer = new AlchemyWebSigner({
  client: {
    connection: {
      rpcUrl: "/api/rpc",
    },
    iframeConfig: {
      iframeContainerId: "alchemy-signer-iframe-container",
    },
  },
});
```

## Parameters

### params

`AlchemySignerParams`
The parameters for the Alchemy signer, including the client and session configuration
