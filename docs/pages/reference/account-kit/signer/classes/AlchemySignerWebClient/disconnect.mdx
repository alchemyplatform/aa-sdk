---
title: disconnect
description: Overview of the disconnect method
slug: wallets/reference/account-kit/signer/classes/AlchemySignerWebClient/disconnect
---



Asynchronous function that clears the user and resets the iframe stamper.

## Import

```ts
import { AlchemySignerWebClient } from "@account-kit/signer";
```

## Usage

```ts
import { AlchemySignerWebClient } from "@account-kit/signer";

const client = new AlchemySignerWebClient({
  connection: {
    apiKey: "your-api-key",
  },
  iframeConfig: {
    iframeContainerId: "signer-iframe-container",
  },
});

const account = await client.disconnect();
```
