---
title: createAlchemyPublicRpcClient
description: Creates an Alchemy public RPC client with the provided chain, connection configuration, and optional fetch options. The client has alchemy methods and can dynamically update HTTP headers.
slug: wallets/reference/account-kit/core/variables/createAlchemyPublicRpcClient
layout: reference
---

{/* This file is auto-generated by TypeDoc. Do not edit manually. */}

```ts
const createAlchemyPublicRpcClient: ({
  transport,
  chain,
}) => ClientWithAlchemyMethods;
```

Defined in: account-kit/infra/dist/types/client/rpcClient.d.ts:26

Creates an Alchemy public RPC client with the provided chain, connection configuration, and optional fetch options. The client has alchemy methods and can dynamically update HTTP headers.

## Example

```ts
import { createAlchemyPublicRpcClient, alchemy } from "@account-kit/infra";
import { sepolia } from "@account-kit/infra";

const client = createAlchemyPublicRpcClient({
  transport: alchemy({
    apiKey: "ALCHEMY_API_KEY",
  }),
  chain: sepolia,
});
```

## Parameters

| Parameter                         | Type                                                       |
| :-------------------------------- | :--------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| `{ transport, chain, }`           | \{ `chain`: `Chain`                                        | `undefined`; `transport`: [`AlchemyTransport`](../type-aliases/AlchemyTransport.mdx); } |
| `{ transport, chain, }.chain`     | `Chain`                                                    | `undefined`                                                                             |
| `{ transport, chain, }.transport` | [`AlchemyTransport`](../type-aliases/AlchemyTransport.mdx) |

## Returns

[`ClientWithAlchemyMethods`](../type-aliases/ClientWithAlchemyMethods.mdx)

A client object tailored with Alchemy methods and capabilities to interact with the blockchain
