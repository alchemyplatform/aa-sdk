---
title: createAccount
description: Overview of the createAccount function
slug: wallets/reference/account-kit/core/src/functions/createAccount
layout: reference
---

```ts
function createAccount<TAccount>(params, config): Promise<any>;
```

Defined in: [account-kit/core/src/actions/createAccount.ts:78](https://github.com/alchemyplatform/aa-sdk/blob/4987bdd63823427c48ec21401b71fa3cfc2d0675/account-kit/core/src/actions/createAccount.ts#L78)

Creates an account of a specified type using the provided parameters and configuration. Supports creating LightAccount and MultiOwnerModularAccount types.

## Example

```ts
import { createAccount } from "@account-kit/core";
// see createConfig for more information on how to create a config
import { config } from "./config";

const account = createAccount(
  {
    type: "LightAccount",
  },
  config,
);
```

## Type Parameters

<table>
  <thead>
    <tr>
      <th align="left">Type Parameter</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `TAccount` *extends* [`SupportedAccountTypes`](../type-aliases/SupportedAccountTypes.mdx)
      </td>
    </tr>
  </tbody>
</table>

## Parameters

<table>
  <thead>
    <tr>
      <th align="left">Parameter</th>
      <th align="left">Type</th>
      <th align="left">Description</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `params`
      </td>

      <td>
        [`CreateAccountParams`](../type-aliases/CreateAccountParams.mdx)\<`TAccount`>
      </td>

      <td>
        The parameters required to create the account, including the type and account parameters
      </td>
    </tr>

    <tr>
      <td>
        `config`
      </td>

      <td>
        [`AlchemyAccountsConfig`](../type-aliases/AlchemyAccountsConfig.mdx)
      </td>

      <td>
        The configuration object for Alchemy accounts
      </td>
    </tr>

  </tbody>
</table>

## Returns

`Promise`\<`any`>

A promise that resolves to the created account object
