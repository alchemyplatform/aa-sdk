---
title: account-kit/core/src
description: Overview of account-kit/core/src
slug: wallets/reference/account-kit/core/src/README
layout: reference
---

## Classes

<table>
  <thead>
    <tr>
      <th align="left">Class</th>
      <th align="left">Description</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        [ClientOnlyPropertyError](classes/ClientOnlyPropertyError.mdx)
      </td>

      <td>
        Error thrown when a client only property is accessed on the server
      </td>
    </tr>

  </tbody>
</table>

## Type Aliases

<table>
  <thead>
    <tr>
      <th align="left">Type Alias</th>
      <th align="left">Description</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        [AccountConfig](type-aliases/AccountConfig.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [AccountState](type-aliases/AccountState.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [AlchemyAccountsConfig](type-aliases/AlchemyAccountsConfig.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [AlchemyClientState](type-aliases/AlchemyClientState.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [AlchemySigner](type-aliases/AlchemySigner.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [AlchemySignerClient](type-aliases/AlchemySignerClient.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [BaseCreateConfigProps](type-aliases/BaseCreateConfigProps.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [ClientActions](type-aliases/ClientActions.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [ClientStoreConfig](type-aliases/ClientStoreConfig.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [Connection](type-aliases/Connection.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [CreateAccountParams](type-aliases/CreateAccountParams.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [CreateConfigProps](type-aliases/CreateConfigProps.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [GetAccountParams](type-aliases/GetAccountParams.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [GetAccountResult](type-aliases/GetAccountResult.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [GetSmartAccountClientParams](type-aliases/GetSmartAccountClientParams.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [GetSmartAccountClientResult](type-aliases/GetSmartAccountClientResult.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [GetSmartWalletClientParams](type-aliases/GetSmartWalletClientParams.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [GetSmartWalletClientResult](type-aliases/GetSmartWalletClientResult.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [GetUserResult](type-aliases/GetUserResult.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [SignerStatus](type-aliases/SignerStatus.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [SolanaConnection](type-aliases/SolanaConnection.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [Store](type-aliases/Store.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [StoredState](type-aliases/StoredState.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [StoreState](type-aliases/StoreState.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [SupportedAccount](type-aliases/SupportedAccount.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [SupportedAccounts](type-aliases/SupportedAccounts.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [SupportedAccountTypes](type-aliases/SupportedAccountTypes.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

  </tbody>
</table>

## Variables

<table>
  <thead>
    <tr>
      <th align="left">Variable</th>
      <th align="left">Description</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        [accountLoupeActions](variables/accountLoupeActions.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [DEFAULT\_IFRAME\_CONTAINER\_ID](variables/DEFAULT_IFRAME_CONTAINER_ID.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

  </tbody>
</table>

## Functions

<table>
  <thead>
    <tr>
      <th align="left">Function</th>
      <th align="left">Description</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        [convertSignerStatusToState](functions/convertSignerStatusToState.mdx)
      </td>

      <td>
        Converts the AlchemySigner's status to a more readable object
      </td>
    </tr>

    <tr>
      <td>
        [cookieStorage](functions/cookieStorage.mdx)
      </td>

      <td>
        Function to create cookie based Storage
      </td>
    </tr>

    <tr>
      <td>
        [cookieToInitialState](functions/cookieToInitialState.mdx)
      </td>

      <td>
        Converts a cookie into an initial state object
      </td>
    </tr>

    <tr>
      <td>
        [createAccount](functions/createAccount.mdx)
      </td>

      <td>
        Creates an account of a specified type using the provided parameters and configuration. Supports creating LightAccount and MultiOwnerModularAccount types.

        **Example**

        ```ts
        import { createAccount } from "@account-kit/core";
        // see createConfig for more information on how to create a config
        import { config } from "./config";

        const account = createAccount({
         type: "LightAccount",
        }, config);
        ```
      </td>
    </tr>

    <tr>
      <td>
        [createConfig](functions/createConfig.mdx)
      </td>

      <td>
        Creates an AlchemyAccountsConfig object that can be used in conjunction with
        the actions exported from `@account-kit/core`.

        The config contains core and client stores that can be used to manage account state
        in your application.

        **Example**

        ```ts
        import { createConfig } from "@account-kit/core";
        import { sepolia } from "@account-kit/infra";

        const config = createConfig({
         chain: sepolia,
         transport: alchemy({ apiKey: "your-api-key" }),
        });
        ```
      </td>
    </tr>

    <tr>
      <td>
        [createDefaultAccountState](functions/createDefaultAccountState.mdx)
      </td>

      <td>
        Creates the default account state for the given chains.
      </td>
    </tr>

    <tr>
      <td>
        [createSigner](functions/createSigner.mdx)
      </td>

      <td>
        Given initial client store parameters, it initializes an AlchemySigner instance.
        This should only be called on the client.
      </td>
    </tr>

    <tr>
      <td>
        [defaultAccountState](functions/defaultAccountState.mdx)
      </td>

      <td>
        Returns the default state for an account of a supported type.

        **Example**

        ```ts
        import { defaultAccountState } from "@account-kit/core";

        const defaultLightAccountState = defaultAccountState<"LightAccount">();
        ```
      </td>
    </tr>

    <tr>
      <td>
        [disconnect](functions/disconnect.mdx)
      </td>

      <td>
        Disconnects the current signer, accounts, and clears the store.

        **Example**

        ```ts
        import { disconnect, createConfig } from "@account-kit/core";
        import { sepolia } from "@account-kit/infra";

        const config = createConfig({
         chain: sepolia,
         apiKey: "your-api-key",
        });

        await disconnect(config);
        ```
      </td>
    </tr>

    <tr>
      <td>
        [getAccount](functions/getAccount.mdx)
      </td>

      <td>
        Retrieves the account of the specified type from the client store based on the provided configuration.

        **Example**

        ```ts
        import { getAccount } from "@account-kit/core";
        // see createConfig for more information on how to create a config
        import { config } from "./config";

        const { account, status } = getAccount({
         type: "LightAccount"
        }, config)
        ```
      </td>
    </tr>

    <tr>
      <td>
        [getBundlerClient](functions/getBundlerClient.mdx)
      </td>

      <td>
        Retrieves the BundlerClient from the core store of the given AlchemyAccountsConfig.

        **Example**

        ```ts
        // see `createConfig` for more information on how to create a config
        import { config } from "./config";

        const bundlerClient = getBundlerClient(config);
        ```
      </td>
    </tr>

    <tr>
      <td>
        [getChain](functions/getChain.mdx)
      </td>

      <td>
        Gets the currently active chain
      </td>
    </tr>

    <tr>
      <td>
        [getConnection](functions/getConnection.mdx)
      </td>

      <td>
        Used to get the connection for the currently active chain

        **Example**

        ```ts
        import { getConnection } from "@account-kit/core";
        import { config } from "./config";

        const connection = getConnection(config);
        ```
      </td>
    </tr>

    <tr>
      <td>
        [getSigner](functions/getSigner.mdx)
      </td>

      <td>
        If there is a signer attached to the client state, it will return it.
        The signer should always be null on the server, and will be set on the client
        if the store was properly hydrated.

        **Example**

        ```ts
        import { getSigner } from "@account-kit/core";
        import { config } from "./config";

        const signer = getSigner(config);
        ```
      </td>
    </tr>

    <tr>
      <td>
        [getSignerStatus](functions/getSignerStatus.mdx)
      </td>

      <td>
        Retrieves the signer status from the client's store in the provided configuration.

        **Example**

        ```ts
        import { getSignerStatus } from "@account-kit/core";
        // see createConfig for more information on how to create a config
        import { config } from "./config";

        const signerStatus = getSignerStatus(config);
        ```
      </td>
    </tr>

    <tr>
      <td>
        [~~getSmartAccountClient~~](functions/getSmartAccountClient.mdx)
      </td>

      <td>
        Obtains a smart account client based on the provided parameters and configuration. Supports creating any of the SupportAccountTypes in Account Kit.
        If the signer is not connected, or an account is already being intializes, this results in a loading state.

        **Deprecated**

        Use `getSmartWalletClient` instead.

        **Example**

        ```ts
        import { getSmartAccountClient } from "@account-kit/core";
        // see createConfig for more information on how to create a config
        import { config } from "./config";

        const { client, address, isLoadingClient } = getSmartAccountClient({
         type: "LightAccount",
        }, config);
        ```
      </td>
    </tr>

    <tr>
      <td>
        [getSmartWalletClient](functions/getSmartWalletClient.mdx)
      </td>

      <td>
        Creates and returns a Smart Wallet Client instance.
        Returns undefined if running in a server environment or if no signer is connected.
        Caches clients by chain ID & address for performance optimization.

        **Example**

        ```ts
        import { getSmartWalletClient } from "@account-kit/core";
        // see createConfig for more information on how to create a config
        import { config } from "./config.js";

        const client = getSmartWalletClient(config);

        // With specific account address
        const clientWithAccount = getSmartWalletClient(config, {
          account: "0x1234..."
        });
        ```
      </td>
    </tr>

    <tr>
      <td>
        [getSolanaConnection](functions/getSolanaConnection.mdx)
      </td>

      <td>
        Used to get the connection for the id

        **Example**

        ```ts
        import { getSolanaConnection } from "@account-kit/core";
        import { config } from "./config";

        const connection = getSolanaConnection(config);
        ```
      </td>
    </tr>

    <tr>
      <td>
        [getUser](functions/getUser.mdx)
      </td>

      <td>
        Returns the currently logged in user if using an SCA with the AlchemySigner
        or the connected EOA details.

        **Example**

        ```ts
        import { getUser } from "@account-kit/core";
        import { config } from "./config";

        const user = getUser(config);
        ```
      </td>
    </tr>

    <tr>
      <td>
        [hydrate](functions/hydrate.mdx)
      </td>

      <td>
        Will hydrate the client store with the provided initial state if one is provided.

        **Example**

        ```ts
        import { hydrate, cookieToInitialState } from "@account-kit/core";
        import { config } from "./config";

        const initialState = cookieToInitialState(document.cookie);
        const { onMount } = hydrate(config, initialState);
        // call onMount once your component has mounted
        ```
      </td>
    </tr>

    <tr>
      <td>
        [isLightAccountParams](functions/isLightAccountParams.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [isModularV2AccountParams](functions/isModularV2AccountParams.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [isMultiOwnerLightAccountParams](functions/isMultiOwnerLightAccountParams.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [isMultiOwnerModularAccountParams](functions/isMultiOwnerModularAccountParams.mdx)
      </td>

      <td>
        ‐
      </td>
    </tr>

    <tr>
      <td>
        [parseCookie](functions/parseCookie.mdx)
      </td>

      <td>
        Helper function that can be used to parse a cookie string on the server or client
      </td>
    </tr>

    <tr>
      <td>
        [reconnect](functions/reconnect.mdx)
      </td>

      <td>
        This method will use the current state in the client store and attempt to restore
        connected instances of previously used accounts and the signer.

        **Example**

        ```ts
        import { reconnect } from "@account-kit/core";
        import { config } from "./config";

        await reconnect(config);
        ```
      </td>
    </tr>

    <tr>
      <td>
        [setChain](functions/setChain.mdx)
      </td>

      <td>
        Allows you to change the current chain in the core store. Note, this chain
        must be one of the chains configured in your original createConfig call.

        **Example**

        ```ts
        import { setChain } from "@account-kit/core";
        import { config } from "./config";
        import { sepolia } from "@account-kit/infra";

        await setChain(config, sepolia);
        ```
      </td>
    </tr>

    <tr>
      <td>
        [watchAccount](functions/watchAccount.mdx)
      </td>

      <td>
        Watches for changes to a specific type of account and triggers the provided callback function when changes occur.

        **Example**

        ```ts
        import { watchAccount } from "@account-kit/core";
        // see createConfig for more information on how to create a config
        import { config } from "./config";

        watchAccount("LightAccount", config)(console.log);
        ```
      </td>
    </tr>

    <tr>
      <td>
        [watchBundlerClient](functions/watchBundlerClient.mdx)
      </td>

      <td>
        Watches for changes to the bundler client within the given configuration and triggers a callback when changes occur.

        **Example**

        ```ts
        import { watchBundlerClient } from "@account-kit/core";
        // see createConfig for more information on how to create a config
        import { config } from "./config";

        watchBundlerClient(config)(console.log);
        ```
      </td>
    </tr>

    <tr>
      <td>
        [watchChain](functions/watchChain.mdx)
      </td>

      <td>
        Allows you to subscribe to changes of the chain in the client store.

        **Example**

        ```ts
        import { watchChain } from "@account-kit/core";
        // see createConfig for more information on how to create a config
        import { config } from "./config";

        watchChain(config)(console.log);
        ```
      </td>
    </tr>

    <tr>
      <td>
        [watchConnection](functions/watchConnection.mdx)
      </td>

      <td>
        Subscribe to changes to the active connection

        **Example**

        ```ts
        import { watchConnection } from "@account-kit/core";
        // see createConfig for more information on how to create a config
        import { config } from "./config";

        watchConnection(config)(console.log);
        ```
      </td>
    </tr>

    <tr>
      <td>
        [watchSigner](functions/watchSigner.mdx)
      </td>

      <td>
        Subscribe to changes of the signer instance on the client store.

        **Example**

        ```ts
        import { watchSigner } from "@account-kit/core";
        // see createConfig for more information on how to create a config
        import { config } from "./config";

        watchSigner(config)(console.log);
        ```
      </td>
    </tr>

    <tr>
      <td>
        [watchSignerStatus](functions/watchSignerStatus.mdx)
      </td>

      <td>
        Watches the signer status in the client store and triggers the provided callback function when the status changes.

        **Example**

        ```ts
        import { watchSignerStatus } from "@account-kit/core";
        // see createConfig for more information on how to create a config
        import { config } from "./config";

        watchSignerStatus(config)(console.log);
        ```
      </td>
    </tr>

    <tr>
      <td>
        [watchSmartAccountClient](functions/watchSmartAccountClient.mdx)
      </td>

      <td>
        Watches for changes to the smart account client and triggers the provided callback when a change is detected.

        **Example**

        ```ts
        import { watchSmartAccountClient } from "@account-kit/core";
        // see createConfig for more information on how to create a config
        import { config } from "./config";

        watchSmartAccountClient({ type: "LightAccount" }, config)(console.log);
        ```
      </td>
    </tr>

    <tr>
      <td>
        [watchSmartWalletClient](functions/watchSmartWalletClient.mdx)
      </td>

      <td>
        Creates a subscription function that watches for changes to the Smart Wallet Client.
        Triggers the onChange callback whenever the signer status or chain changes.

        **Example**

        ```ts
        import { watchSmartWalletClient } from "@account-kit/core";
        // see createConfig for more information on how to create a config
        import { config } from "./config.js";

        const watchClient = watchSmartWalletClient(config);
        const unsubscribe = watchClient((client) => {
          console.log("Smart Wallet Client changed:", client);
        });

        // Clean up subscription
        unsubscribe();
        ```
      </td>
    </tr>

    <tr>
      <td>
        [watchSolanaConnection](functions/watchSolanaConnection.mdx)
      </td>

      <td>
        Subscribe to changes to the solana connection for the id

        **Example**

        ```ts twoslash
        import { watchSolanaConnection } from "@account-kit/core";
        // see createConfig for more information on how to create a config
        const config = {} as any;

        watchSolanaConnection(config)(console.log);
        ```
      </td>
    </tr>

    <tr>
      <td>
        [watchUser](functions/watchUser.mdx)
      </td>

      <td>
        Watches for changes to the user in the client store and triggers the provided callback when a change is detected.

        **Example**

        ```ts
        import { watchUser } from "@account-kit/core";
        // see createConfig for more information on how to create a config
        import { config } from "./config";

        watchUser(config)(console.log);
        ```
      </td>
    </tr>

  </tbody>
</table>

## References

### AlchemySmartAccountClient

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### AlchemySmartAccountClientConfig

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### createAlchemySmartAccountClient

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### createLightAccount

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### createModularAccount

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### createMultiOwnerLightAccount

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### createMultisigModularAccount

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### getMAInitializationData

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### getMSCAUpgradeToData

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### internalCreateWebSigner

Renames and re-exports [createSigner](functions/createSigner.mdx)

---

### lightAccountClientActions

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### multiOwnerLightAccountClientActions

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### MultiOwnerPlugin

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### multiOwnerPluginActions

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### MultisigPlugin

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### MultisigPluginAbi

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### multisigPluginActions

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### MultisigPluginExecutionFunctionAbi

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### multisigSignatureMiddleware

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### pluginManagerActions

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### SessionKeyAccessListType

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### SessionKeyPermissionsBuilder

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### SessionKeyPlugin

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### SessionKeyPluginAbi

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### sessionKeyPluginActions

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### SessionKeyPluginExecutionFunctionAbi

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)

---

### SessionKeySigner

Renames and re-exports [accountLoupeActions](variables/accountLoupeActions.mdx)
