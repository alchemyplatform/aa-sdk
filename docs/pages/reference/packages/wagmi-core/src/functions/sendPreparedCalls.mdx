---
title: sendPreparedCalls
description: Overview of the sendPreparedCalls function
slug: wallets/reference/wagmi-core/functions/sendPreparedCalls
layout: reference
---

{/* This file is auto-generated by TypeDoc. Do not edit manually. */}

```ts
function sendPreparedCalls(config, parameters): Promise<{
  id: `0x${string}`;
}>;
```

Defined in: [packages/wagmi-core/src/actions/sendPreparedCalls.ts:39](https://github.com/alchemyplatform/aa-sdk/blob/62cd70f7828f50c0b151e2745908473e90a0cedb/packages/wagmi-core/src/actions/sendPreparedCalls.ts#L39)

Signs and sends prepared calls.

This function takes prepared calls (from `prepareSwap` or `prepareCalls`), signs them,
and executes the transaction. Returns prepared call IDs that can be used to track the
execution status.

Notice: If you do not wish to inspect the prepared call data, you can
simply use `sendCalls` instead, which combines preparing, signing, and
sending in a single action.

## Parameters

<table>
  <thead>
    <tr>
      <th align="left">Parameter</th>
      <th align="left">Type</th>
      <th align="left">Description</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>
        `config`
      </td>

      <td>
        `Config`
      </td>

      <td>
        The Wagmi config
      </td>
    </tr>

    <tr>
      <td>
        `parameters`
      </td>

      <td>
        [`SendPreparedCallsParameters`](../type-aliases/SendPreparedCallsParameters.mdx)
      </td>

      <td>
        Prepared calls result and optional connector
      </td>
    </tr>
  </tbody>
</table>

## Returns

`Promise`\<\{
`id`: `` `0x${string}` ``;
}>

Promise that resolves to an object containing the call ID for tracking execution status

## Throws

Throws if the wallet is not an Alchemy smart wallet
