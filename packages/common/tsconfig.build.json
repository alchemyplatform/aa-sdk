{
  "extends": "typescript-template/build.json",
  "exclude": [
    "node_modules",
    "**/*/__tests__",
    "**/*/*.test.ts",
    "**/*/*.test-d.ts",
    "**/*/*.e2e.test.ts",
    "vitest.config.ts",
    "vitest.config.e2e.ts"
  ],
  "include": ["src"],
  "compilerOptions": {
    "sourceMap": true,
    "jsx": "react-jsx",
    // Vitest 4's `vitest/globals` no longer transitively provides Node's
    // ambient types (it did in v2); the logging helpers reference `process`.
    "types": ["vitest/globals", "node"]
  }
}
